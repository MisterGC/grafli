[project]
name = "whiteboard"
version = "0.1.0"
description = "Lightweight desktop whiteboard with plain-text .board files"
requires-python = ">=3.12"
dependencies = ["PySide6>=6.7"]

[project.scripts]
whiteboard = "whiteboard.app:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
