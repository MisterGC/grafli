# GraphQL Federation Notes

This resource is linked from the `graphql` node in `architecture.grafli`.

Use markdown resources for evidence and rationale that are useful but too
detailed for the first-read canvas.

- Schema stitching happens at the API boundary.
- Resolver edges use `data:` and `risk:` labels on the main graph.
- Contract tests verify the API shape from the operations layer.
